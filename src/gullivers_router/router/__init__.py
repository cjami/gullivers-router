"""Runtime router: single-call routing between local and cloud models."""

from __future__ import annotations

__all__ = ["run"]


def run() -> None:
    """Start the runtime routing loop (embed -> route -> answer)."""
    print("Router runtime is not implemented yet.")
